import 'package:flutter/material.dart';

class MutableIncidentCard extends StatelessWidget {
  const MutableIncidentCard({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 270,
      color: Colors.red,
    );
  }
}
